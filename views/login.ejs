<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Dmail - Login</title>
</head>

<body>
    <nav>
        <h4>My Dmail</h4>
        <ul>
            <li><a href="/login">Login</a></li>
            <li><a href="/register">Register</a></li>
        </ul>
    </nav>

    <div class="container">
        <div class="table">
            <div class="tableWord">
                Login Form
            </div>
            <div class="tableBody">
                <form action="/auth/login" method="POST">

                    <div class="form">
                        <label for="email">Email Address</label>
                        <input type="email" class="form1" id="email" name="email">
                    </div>

                    <div class="form">
                        <label for="password">Password</label>
                        <input type="password" class="form1" id="password" name="password">
                    </div>

                    <button type="submit" class="btn">Login</button>
                </form>
            </div>
        </div>

        <% if(typeof message !="undefined" && message) { %>
            <div class="mess">
                <div class="alert">
                    <strong>
                        <%= message %>
                    </strong>
                </div>
            </div>
            <% } %>
    </div>

</body>

</html>