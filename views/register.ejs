<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Dmail - Register</title>
</head>

<body>

    <nav>
        <h1>My Dmail</h4>
            <ul>
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>
            </ul>
    </nav>

    <div class="container">
        <div class="table">
            <div class="tableWord">
                Register Form
            </div>
            <div class="tableBody">
                <form action="/auth/register" method="POST">
                    <div class="form">
                        <label for="name">Name</label>
                        <input type="text" class="form1" id="name" name="name">
                    </div>

                    <div class="form">
                        <label for="email">Email Address</label>
                        <input type="email" class="form1" id="email" name="email">
                        <div>We'll never share your email with anyone else.</div>
                    </div>

                    <div class="form">
                        <label for="password">Password</label>
                        <input type="password" class="form1" id="password" name="password">
                    </div>

                    <div class="form">
                        <label for="passwordConfirm">Confirm Password</label>
                        <input type="password" class="form1" id="passwordConfirm" name="passwordConfirm">
                    </div>

                    <button type="submit" class="btn">Register User</button>
                </form>
            </div>
        </div>



        <% if (typeof message !=="undefined" && message) { %>
            <div class="mess">
                <% if (message==="User registered" ) { %>
                    <div class="alert2">
                        <strong>
                            <%= message %>. <a href="/login">Sign in here</a>
                        </strong>
                    </div>
                    <% } else { %>
                        <div class="alert">
                            <strong>
                                <%= message %>
                            </strong>
                        </div>
                        <% } %>
            </div>
            <% } %>

    </div>
</body>

</html>